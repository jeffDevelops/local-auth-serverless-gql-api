{
  "name": "api4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/concurrently \"npm run build:watch\" \"./node_modules/.bin/vercel dev --listen 4000\"",
    "dev": "npm run build && npm run migration:run && npm run build:watch",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc --project .",
    "now-build": "npm run migration:run && npm run build",
    "build:watch": "tsc --watch --project .",
    "deploy": "npm run build && ./node_modules/.bin/vercel",
    "migration:run": "./node_modules/.bin/ts-node ./node_modules/.bin/typeorm migration:run --config ./ormconfig.ts",
    "migration:revert": "./node_modules/.bin/ts-node ./node_modules/.bin/typeorm migration:revert --config ./ormconfig.ts",
    "migration:create": "./node_modules/.bin/ts-node ./node_modules/.bin/typeorm migration:create -d src/migrations",
    "codegen:schema": "./node_modules/.bin/apollo-codegen"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@now/node": "^1.5.1",
    "apollo-server-micro": "^2.12.0",
    "bcryptjs": "^2.4.3",
    "class-validator": "^0.14.0",
    "dotenv": "^8.2.0",
    "graphql": "^14.1.1",
    "jsonwebtoken": "^8.5.1",
    "micro-cors": "^0.1.1",
    "pg": "^8.0.3",
    "reflect-metadata": "^0.1.13",
    "type-graphql": "^0.17.6",
    "typeorm": "^0.2.24",
    "vercel": "^19.1.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/express": "^4.17.6",
    "@types/graphql": "^14.5.0",
    "@types/jsonwebtoken": "^8.3.9",
    "@types/micro-cors": "^0.1.0",
    "@types/node": "^13.13.2",
    "concurrently": "^5.2.0",
    "ts-node": "^8.9.0",
    "ts-node-dev": "^1.0.0-pre.44",
    "typescript": "^3.8.3"
  }
}
